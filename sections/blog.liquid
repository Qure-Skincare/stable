{% assign blog_handle = blog.handle %}
{% unless blog_handle == 'light-therapy' or blog_handle == 'skin' or blog_handle == 'skincare' or blog_handle == 'all' %}
    {% assign blog_handle = 'all' %}
{% endunless %}

<link rel="stylesheet" href="https://qureskincaredns-stable.com/sections/new/blog-section/blog-section.css" />
<section class="blog-section">
    <div class="container">
        <div class="blog-section__sections">
            <h2>{{ 'blog.choose.header' | t }}</h2>
            <ul>
                {% for link in linklists.blog-menu.links offset: 1 limit: 3 %}
                    <li>
                        <a href="{{ link.url }}">
                            <picture>
                                {% case forloop.index %}
                                    {% when 1 %}
                                        <source media="(min-width:768px)" srcset="{{ 'blog.blog-post.settings.image_1' | t }}" />
                                        <img src="{{ 'blog.blog-post.settings.image_mobile_1' | t }}" alt="{{ link.title | escape }}" width="256" height="170" />
                                    {% when 2 %}
                                        <source media="(min-width:768px)" srcset="{{ 'blog.blog-post.settings.image_2' | t }}" />
                                        <img src="{{ 'blog.blog-post.settings.image_mobile_2' | t }}" alt="{{ link.title | escape }}" width="256" height="170" />
                                    {% when 3 %}
                                        <source media="(min-width:768px)" srcset="{{ 'blog.blog-post.settings.image_3' | t }}" />
                                        <img src="{{ 'blog.blog-post.settings.image_mobile_3' | t }}" alt="{{ link.title | escape }}" width="256" height="170" />
                                {% endcase %}
                            </picture>
                            <h6>{{ link.title }}</h6>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
        <div class="blog-section__posts">
            <h2>{{ 'blog.choose.articles' | t }}</h2>
            {%- paginate blogs[blog_handle].articles by 9 -%}
                <ul>
                    {% for article in blogs[blog_handle].articles %}
                        {% render 'n-blog-snippet-post', article: article %}
                    {% endfor %}
                </ul>
                {% render 'n-blog-snippet-pagination', paginate: paginate %}
            {%- endpaginate -%}
        </div>
        <div class="blog-section__top-list">
            <h6>{{ 'blog.popular.header' | t }}</h6>
            <ul class="e-top-posts">
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skin', limit: 1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skincare', limit: 1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'light-therapy', limit: 1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skin', limit: 1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skincare', limit: 1, offset:1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skincare', limit: 1, offset:2 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'light-therapy', limit: 1, offset:1 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'light-therapy', limit: 1, offset:2 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skin', limit: 1, offset:2 %}
                {% render 'n-blog-snippet-post-popular' with blog_handle: 'skin', limit: 1, offset:3 %}
            </ul>
        </div>
    </div>
</section>